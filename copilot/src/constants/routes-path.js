class RoutesPath {
    static HOME = { NAME: 'Home', PATH: '/' };

    static EXAMPLE_PAGE = { NAME: 'Example Page', PATH: '/example' };
    
    static CHAT_SUGGESTIONS = { NAME: 'Chat Suggestions', PATH: '/ChatSuggestions' };
}

export default RoutesPath;
